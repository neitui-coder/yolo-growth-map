# 小程序主体记录

> 记录时间：2026-04（Sean 口述）

## 当前在用（开发中）
- **AppID**: `wx2d60e117f613f2be`
- **主体**: Sean 自有 / 当前开发主体
- **CloudBase 环境**: `cloud1-7giblg7i4595eaf3`
- 全部代码、云函数、数据库、媒体均在此主体下
- 开发流程顺畅（无需频繁扫码授权）

## 备选主体（杨三角公众号下新建）
- **小程序原始 ID（gh_id）**: `gh_af1dbdf12929`
- **主体**: 杨三角公众号下新建的小程序主体
- 用途待定 —— 见下方决策

## 待决策：rebrand vs 迁移
两个方案：
1. **方案 A — 当前主体直接改名为「优路 / YOLO」**（推荐）
   - 无迁移成本，开发流程不变
   - CloudBase 环境、DB、openId 绑定全部保留
2. **方案 B — 迁移到杨三角主体 `gh_af1dbdf12929`**
   - 官方归属正规，可挂杨三角公众号
   - 成本高：openId 是按 appid 隔离的，迁移后**所有现有微信绑定全部失效**，
     需重新绑定每个用户；CloudBase 环境要在新主体下重建并迁数据/媒体；
     开发需频繁找人扫码授权，节奏受影响

**关键技术约束**：`openId` 与 appid 绑定。换主体 = 现有 auth/bind 体系（adminOpenIds、
用户身份绑定）全部作废重来。除非有杨三角官方上线的硬性业务要求，否则建议留在当前主体。
